@model TeamGeneratorApp.Models.ViewModels.EventVM

@{
    ViewBag.Title = "Details";
}
<br />
<p>
    @Html.ActionLink("< Back to category", "Details", "Categories", new { id = ViewBag.CategoryId }, null)
</p>
<h2>@Html.DisplayFor(model => model.Name)</h2>
<hr />
<h4>Event</h4>
<br />
<div>
    <dl class="dl-horizontal">

        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Start)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Start)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Finish)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Finish)
        </dd>

        <dt>

        </dt>
        <dd>
            @*@if (ViewBag.CanGenerate)
            {
                <button>@Html.ActionLink("Generate teams", "GenerateTeams", new { eventId = Model.Id })</button>
            }*@
        </dd>

    </dl>
</div>
<div id="tabs">
    <ul class="nav nav-tabs">
        <li class="active">
            <a href="#Users" data-toggle="tab">Users</a>
        </li>
        <li>
            <a href="#Votings" data-toggle="tab">Votings</a>
        </li>
        <li>
            <a href="#Generators" data-toggle="tab">Generators</a>
        </li>
    </ul>

    <div class="tab-content">
        <div class="tab-pane active" id="Users">
            @Html.Action("UsersGrid", "Events", new { eventId = Model.Id })
        </div>
        <div class="tab-pane" id="Votings">
            @Html.Action("VotingsGrid", "Events", new { eventId = Model.Id })
        </div>
        <div class="tab-pane" id="Generators">
            @Html.Action("GeneratorsGrid", "Events", new { eventId = Model.Id })
        </div>
    </div>

</div>